---
phase: 03-churn-domain
task: 3
total_tasks: 4
status: in_progress
last_updated: 2026-05-31T02:11:57.057Z
---

<current_state>
Plan 03-04 (Churn page) is at Task 3 — a blocking human-verify checkpoint.
Tasks 1 and 2 are fully committed (commits 655c22b and 1efb8e9, 77/77 tests pass).
Task 3 requires the user to verify the Churn page in the browser.

The what-if chart had TWO sequential bugs that were fixed during this checkpoint:
1. Overlapping stackgroups → fixed with side-by-side make_subplots (commit 1e76686)
2. shared_yaxes=True caused cross-panel stacking → fixed with shared_yaxes=False (commit a7e5ebb)

The user has NOT yet confirmed whether the second fix (a7e5ebb) works.
The checkpoint is still open — waiting for "approved" or issue report.

There are also uncommitted modifications from GSD state updates:
- .planning/REQUIREMENTS.md (updated by executor agents)
- CLAUDE.md (marked as M — may have partial edit from plan frontmatter)
- CONTRIBUTING.md
- README.md
These are normal GSD artifact updates; they do NOT reflect Task 4 (doc update) which hasn't run yet.
</current_state>

<completed_work>
Plans 03-01, 03-02, 03-03: ALL COMPLETE with SUMMARY.md files.

Plan 03-04:
- Task 1: Churn page skeleton (config, caches, control strip, KPI strip, Overview tab + Sankey + period scrubber) — commit 655c22b ✓
- Task 2: What-If tab (accordion sliders, live before/after chart, impact card) + smoke test in test_page_import.py — commit 1efb8e9 ✓
- Bug fix 1: build_whatif_chart overlapping stackgroups → side-by-side subplots — commit 1e76686 ✓
- Bug fix 2: shared_yaxes=True → shared_yaxes=False (stops cross-panel stacking) — commit a7e5ebb ✓

Test suite: 77/77 passing after all fixes.
</completed_work>

<remaining_work>
- Task 3: Human-verify Churn page in browser — AWAITING APPROVAL
  User needs to:
  1. Run: uv run streamlit run app/Home.py
  2. Open Churn page → select cohort → Run Analysis
  3. Overview tab: verify Sankey renders with proportional ribbons, Churned node RED, period scrubber works
  4. What-If tab: move "→ active" slider from 0.93 to 0.50 → RIGHT "Scenario" panel must show VISIBLY SMALLER green area + VISIBLY LARGER red area (baseline period-12: 44% active / 52% churned; scenario: 28% active / 68% churned)
  5. Type "approved" if all behaviors confirmed

- Task 4 (auto, runs AFTER Task 3 approval): Update CLAUDE.md App Pages table + README.md roadmap
  - CLAUDE.md: change "Pending (Phase 03)" → "✅ Implemented (Phase 03)" for Churn row
  - README.md: change "- [ ] **Phase 03**" → "- [x] **Phase 03** — ... page live at /Churn"

- After Task 4: Phase verification (gsd-verifier spawned by orchestrator), then ROADMAP/STATE updates.
</remaining_work>

<decisions_made>
- build_whatif_chart uses make_subplots(shared_yaxes=False) — side-by-side panels so each stackgroup is independent per y-axis
- Diagnostic confirmed: simulate_scenario produces visually distinct data (active: 0.441→0.277, churned: 0.522→0.680 at period 12 with 43pp override)
- The math/narrative was always correct; both chart bugs were purely visual rendering issues
- impact_narrative uses ASCII "->" not unicode arrow (Windows console encoding)
- _cached_scenario uses frozenset(overrides.items()) as cache key — confirmed working (narrative proves cache miss + recompute on slider change)
</decisions_made>

<blockers>
- Human checkpoint: user must verify browser behavior before Task 4 can run
- If chart STILL doesn't show different panels after the shared_yaxes=False fix, investigate whether st.plotly_chart needs a key= parameter that changes with overrides (e.g., key=f"whatif_chart_{hash(frozenset(overrides.items())) % 10000}") to force fresh component mount
</blockers>

<context>
The continuation agent for this checkpoint (spawned by the execute-phase orchestrator) is agent a4495aaa8fe689dd2. However, since the session is being paused, a FRESH continuation agent should be used when resuming — do NOT try to SendMessage to the old agent.

To resume Task 4 after human approval:
1. Read this file + 03-04-PLAN.md Task 4
2. Edit CLAUDE.md: replace "Pending (Phase 03)" line in App Pages table
3. Edit README.md: replace "[ ] **Phase 03**" with "[x] **Phase 03** — ... — page live at /Churn"
4. Run the doc-check verification command from plan Task 4
5. Commit with rtk git
6. Then spawn gsd-verifier for phase verification
7. Then run phase-complete and offer_next routing
</context>

<next_action>
Resume with: Ask user to re-verify the Churn What-If chart in browser (the shared_yaxes=False fix was the last applied change). If "approved", proceed to Task 4 (docs update) then phase verification. Command to use: /gsd:resume-work
</next_action>
