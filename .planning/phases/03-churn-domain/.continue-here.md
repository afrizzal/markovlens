---
phase: 03-churn-domain
task: 3
total_tasks: 4
status: in_progress
last_updated: 2026-05-31T02:11:57.057Z
---

<current_state>
Plan 03-04 (Churn page) is at Task 3 — a blocking human-verify checkpoint.
Tasks 1 and 2 are fully committed. Root cause of the what-if chart not visibly
updating has been identified and fixed in _apply_overrides.

Bug history:
1. Overlapping stackgroups → fixed with side-by-side make_subplots (commit 1e76686)
2. shared_yaxes=True caused cross-panel stacking → fixed (commit a7e5ebb)
3. ROOT CAUSE: _apply_overrides divided the whole row by row_sum after setting
   the override, normalizing the user's value away (e.g. 0.50 became 0.877).
   Fixed by locking modified cells at target values and redistributing remaining
   mass proportionally to unmodified cells. 4 unit tests added to lock the fix.
   (latest commit — 81/81 tests pass)

The checkpoint is still open — waiting for "approved" or issue report.
</current_state>

<completed_work>
Plans 03-01, 03-02, 03-03: ALL COMPLETE with SUMMARY.md files.

Plan 03-04:
- Task 1: Churn page skeleton (config, caches, control strip, KPI strip, Overview tab + Sankey + period scrubber) — commit 655c22b
- Task 2: What-If tab (accordion sliders, live before/after chart, impact card) + smoke test — commit 1efb8e9
- Bug fix 1: build_whatif_chart overlapping stackgroups — commit 1e76686
- Bug fix 2: shared_yaxes=False — commit a7e5ebb
- Bug fix 3 (ROOT CAUSE): _apply_overrides locks modified cells + 4 tests — latest commit

Test suite: 81/81 passing.
</completed_work>

<remaining_work>
- Task 3: Human-verify Churn page in browser — AWAITING APPROVAL
  User needs to:
  1. Run: uv run streamlit run app/Home.py
  2. Open Churn page → select cohort → Run Analysis
  3. Overview tab: verify Sankey renders with proportional ribbons, Churned node RED, period scrubber works
  4. What-If tab: expand "From Active", move "→ active" slider from 0.93 to 0.50 →
     RIGHT "Scenario" panel must show VISIBLY SMALLER green area + VISIBLY LARGER red area
     (expected: baseline period-12 ~44% active / ~52% churned; scenario ~28% active / ~68% churned)
  5. Type "approved" if all behaviors confirmed

- Task 4 (auto, runs AFTER Task 3 approval): Update CLAUDE.md App Pages table + README.md roadmap
  - CLAUDE.md: change "Pending (Phase 03)" → "✅ Implemented (Phase 03)" for Churn row
  - README.md: change "- [ ] **Phase 03**" → "- [x] **Phase 03** — ... page live at /Churn"

- After Task 4: Phase verification (gsd-verifier spawned by orchestrator), then ROADMAP/STATE updates.
</remaining_work>

<decisions_made>
- build_whatif_chart uses make_subplots(shared_yaxes=False) — side-by-side panels so each stackgroup is independent per y-axis
- _apply_overrides now locks modified cells at target values; remaining mass redistributed proportionally to unmodified cells
- impact_narrative uses ASCII "->" not unicode arrow (Windows console encoding)
- _cached_scenario uses frozenset(overrides.items()) as cache key
</decisions_made>

<blockers>
- Human checkpoint: user must verify browser behavior before Task 4 can run
</blockers>

<context>
To resume Task 4 after human approval:
1. Read this file + 03-04-PLAN.md Task 4
2. Edit CLAUDE.md: replace "Pending (Phase 03)" line in App Pages table
3. Edit README.md: replace "[ ] **Phase 03**" with "[x] **Phase 03** — ... — page live at /Churn"
4. Run the doc-check verification command from plan Task 4
5. Commit with rtk git
6. Then spawn gsd-verifier for phase verification
7. Then run phase-complete and offer_next routing
</context>

<next_action>
Ask user to verify the Churn What-If chart in browser (ROOT CAUSE fix: _apply_overrides now
locks cells at target values). If "approved", proceed to Task 4 (docs update) then phase verification.
</next_action>
