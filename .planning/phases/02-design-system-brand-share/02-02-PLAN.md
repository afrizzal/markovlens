---
phase: 02-design-system-brand-share
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/components/transition_heatmap.py
  - app/components/monte_carlo_fan.py
  - app/components/kpi_card.py
  - app/components/empty_state.py
  - app/components/__init__.py
  - tests/unit/test_components.py
autonomous: true
requirements: [UI-02, BS-02, BS-03]
must_haves:
  truths:
    - "transition_heatmap renders a Plotly heatmap with a fixed [0,1] color scale, per-cell percent annotations, and a warning marker on cells with < 20 observations"
    - "monte_carlo_fan renders a fan chart with P10/P50/P90 bands, a historical/forecast vertical separator, and an explicit legend"
    - "kpi_card and empty_state render custom HTML matching the markov.css component contracts (no st.metric fallback, no Phase 05 TODO)"
  artifacts:
    - path: "app/components/transition_heatmap.py"
      provides: "Annotated heatmap with fixed [0,1] scale + sparsity markers (BS-02)"
      contains: "def transition_heatmap("
    - path: "app/components/monte_carlo_fan.py"
      provides: "Fan chart with P10/P50/P90 bands + separator + legend (BS-03)"
      contains: "def monte_carlo_fan("
    - path: "app/components/kpi_card.py"
      provides: "Custom-HTML KPI card per ui.jsx KPICard contract"
      contains: "def kpi_card("
    - path: "tests/unit/test_components.py"
      provides: "UI-02/BS-02/BS-03 structural assertions on returned figures"
  key_links:
    - from: "app/components/transition_heatmap.py"
      to: "go.Heatmap zmin=0 zmax=1"
      via: "fixed color axis + add_annotation per cell + sparsity ⚠ marker"
      pattern: "zmin\\s*=\\s*0"
    - from: "app/components/monte_carlo_fan.py"
      to: "go.Scatter fill bands + add_vline separator"
      via: "P90→P10(fill)→P50→historical trace order; add_vline at last historical index"
      pattern: "fill"
---

<objective>
Build the reusable component library (UI-02): the new `transition_heatmap` (BS-02) and `monte_carlo_fan` (BS-03) Plotly components, and the extended `kpi_card` and `empty_state` custom-HTML components. All Plotly figure construction lives here (never in domains/). This plan is testable in isolation — components accept plain NumPy/list inputs and call `st.plotly_chart`/`st.markdown`.

Purpose: These are the visual building blocks the Brand Share page wires together. Each is built to the prototype's exact contract so the page is a thin assembly layer.
Output: 2 new components, 2 extended components, updated `__init__.py` re-exports, and one structural test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-brand-share/02-CONTEXT.md
@.planning/phases/02-design-system-brand-share/02-RESEARCH.md
@.planning/phases/02-design-system-brand-share/02-UI-SPEC.md
@.planning/phases/02-design-system-brand-share/02-VALIDATION.md

<interfaces>
<!-- Exact component signatures from UI-SPEC.md §2, §3, §1, §4. Build to these verbatim. -->

transition_heatmap (UI-SPEC §2):
```python
def transition_heatmap(
    matrix: np.ndarray,           # shape (n, n), values in [0, 1]
    obs_counts: np.ndarray,       # shape (n, n), integer observation counts
    state_labels: list[str],      # length n
    *,
    title: str = "Transition probabilities",
    subtitle: str = "Rows = from-state · Columns = to-state",
    period_label: str | None = None,  # e.g. "Period 12 (most recent)"
    height: int = 400,
) -> None:  # calls st.plotly_chart internally
```

monte_carlo_fan (UI-SPEC §3):
```python
def monte_carlo_fan(
    p10: np.ndarray, p50: np.ndarray, p90: np.ndarray,  # each shape (n_steps,)
    *,
    history: np.ndarray | None = None,  # shape (n_hist,)
    brand_label: str = "Brand",
    n_simulations: int = 10_000,
    seed: int = 42,
    height: int = 400,
) -> None:  # calls st.plotly_chart internally
```

kpi_card (UI-SPEC §1) — extend existing stub:
```python
def kpi_card(
    label: str, value: str, *, unit: str | None = None,
    delta: float | None = None, delta_suffix: str = "%",
    sparkline: list[float] | None = None,
    accent: str = "var(--color-primary)",
    icon: str | None = None, tooltip: str | None = None,
) -> None:
```

empty_state (UI-SPEC §4) — extend existing stub:
```python
def empty_state(
    icon: str, title: str, description: str, *,
    action_label: str | None = None,
    action_callback: Callable | None = None,
) -> None:
```

<!-- The Plotly template (markovlens) is registered by 02-01's register_theme().
     These components rely on it being registered by the page before they run, but
     each component must ALSO be testable standalone: tests call register_theme() first. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build transition_heatmap (BS-02) and monte_carlo_fan (BS-03) + failing-first tests</name>
  <files>app/components/transition_heatmap.py, app/components/monte_carlo_fan.py, tests/unit/test_components.py</files>
  <read_first>
    - docs/design-reference/js/charts.jsx (Heatmap line 153-191 + FanChart line 116-149 — visual ground truth)
    - .planning/phases/02-design-system-brand-share/02-UI-SPEC.md (§2 heatmap contract, §3 fan chart contract — exact colors, trace order, annotations)
    - .planning/phases/02-design-system-brand-share/02-RESEARCH.md (Pattern 8 heatmap annotations + luminance threshold; Pattern 9 + Pitfall 3 fan chart fill order)
    - app/styles/plotly_theme.py (HEATMAP_COLORSCALE, CATEGORICAL_COLORWAY constants to reuse)
    - tests/conftest.py (fixtures)
    - .claude/rules/streamlit-conventions.md (Plotly theming, use_container_width=True)
  </read_first>
  <action>
    First, ADD to `tests/unit/test_components.py` (create the file) failing-first tests that import not-yet-existing components and assert on the returned figure structure. Because the components call `st.plotly_chart` internally (return None), refactor each component to build the figure in a private `_build_*_figure(...)` helper that RETURNS the `go.Figure`, and have the public `transition_heatmap`/`monte_carlo_fan` call `st.plotly_chart(fig, use_container_width=True)`. Tests import the `_build_*_figure` helpers directly (no Streamlit runtime needed) and assert structure. At the top of test_components.py: `import streamlit  # noqa: F401` then `from app.styles.plotly_theme import register_theme; register_theme()` in a module-scope fixture so the template exists.

    Tests to write (MUST fail first):
    - `test_transition_heatmap_fixed_scale`: build a 3x3 matrix, obs_counts all 50; `fig = _build_heatmap_figure(...)`; assert the heatmap trace `zmin == 0` and `zmax == 1` (fixed [0,1] scale, never auto-range).
    - `test_transition_heatmap_sparsity`: obs_counts with one cell = 5 (< 20); assert at least one layout annotation has text `"⚠"` and font color `"#D97706"`. Also assert the count of percent-value annotations equals `n*n` (one per cell).
    - `test_monte_carlo_fan_traces`: p10/p50/p90 = monotonic arrays (p10 ≤ p50 ≤ p90 elementwise), history provided; assert `len(fig.data) >= 5` (outer band, inner/median, P10 line, P90 line, historical) and that a `dash`/`line` separator shape exists (`len(fig.layout.shapes) >= 1` OR an add_vline produced a shape). Assert legend entries include names `"Median (P50)"`, `"P10"`, `"P90"`, `"Historical"`.

    Then implement:

    `app/components/transition_heatmap.py` (BS-02):
    - Module docstring; `from __future__ import annotations`; imports `numpy as np`, `plotly.graph_objects as go`, `streamlit as st`, and `from app.styles.plotly_theme import HEATMAP_COLORSCALE`.
    - Module constant `SPARSE_OBS_THRESHOLD: int = 20` (NO magic number; matches `MIN_OBSERVATIONS_PER_CELL`).
    - `_build_heatmap_figure(matrix, obs_counts, state_labels, period_label=None) -> go.Figure`:
      - `go.Heatmap(z=matrix, x=state_labels, y=state_labels, colorscale=HEATMAP_COLORSCALE, zmin=0, zmax=1, showscale=False, hovertemplate=...)`.
      - hovertemplate: `"%{y} → %{x}: %{z:.1%} · %{customdata:,} obs<extra></extra>"` with `customdata=obs_counts`.
      - Per-cell value annotation for every (i, j): text `f"{v*100:.0f}%"` if `v >= 0.10` else `f"{v*100:.1f}%"`; font family `"JetBrains Mono, monospace"`, size 12; text color `"#FFFFFF"` if `v >= 0.55` else `"#0A0A0A"` (luminance threshold per RESEARCH Pattern 8).
      - Sparsity marker: for every (i, j) where `obs_counts[i, j] < SPARSE_OBS_THRESHOLD`, add a second annotation at `x=j, y=i` offset (use `xshift=14, yshift=-12` for top-right) with text `"⚠"`, font color `"#D97706"`, size 10, showarrow False.
      - `fig.update_yaxes(autorange="reversed")` so row 0 is top.
      - Square aspect, height from caller.
    - `transition_heatmap(...)` public fn: render a custom HTML card header via `st.markdown` (`.card .accent-card` wrapper with `--accent: var(--chart-1)`, title in `.t-h3`, subtitle in `.t-xs .t-ter`; if `period_label` given, append `f" · {period_label}"` to subtitle), then `st.plotly_chart(fig, use_container_width=True)`, then a sequential legend caption (`"Low"`→`"High"`) and the sparsity legend `"⚠ Fewer than 20 observations"` in `--color-warning`.

    `app/components/monte_carlo_fan.py` (BS-03):
    - Module docstring; `from __future__ import annotations`; imports numpy, `plotly.graph_objects as go`, streamlit.
    - Module constants for the band fill colors: `BAND_FILL_OUTER = "rgba(67,56,202,0.13)"`, `MEDIAN_COLOR = "#4338CA"` (`--chart-1`), `BAND_LINE_COLOR = "rgba(67,56,202,0.5)"`, `HISTORICAL_COLOR = "#0A0A0A"`, `SEPARATOR_COLOR = "rgba(82,82,91,0.5)"`.
    - `_build_fan_figure(p10, p50, p90, history=None, brand_label="Brand") -> go.Figure`:
      - x-axis indices: historical span `0..len(history)-1` (if history) then forecast span continuing; build x arrays accordingly. Forecast x = `range(n_hist-1, n_hist-1+n_steps)` so the band visually connects to the last historical point.
      - Trace order per UI-SPEC §3 / RESEARCH Pitfall 3: (1) P90 line no-fill, (2) P10 line `fill="tonexty"` fillcolor `BAND_FILL_OUTER` (fills between P10 and the preceding P90), legend name `"P10–P90 range"`, (3) P50 solid `color=MEDIAN_COLOR width=2.5` name `"Median (P50)"`, (4) Historical solid `color=HISTORICAL_COLOR width=2` name `"Historical"` (only if history given), (5) P10 dashed line name `"P10"`, (6) P90 dashed line name `"P90"`. (Either the toself/tonexty single-band or the explicit P10/P90 dotted lines must produce the named legend entries the test checks.)
      - Separator: `fig.add_vline(x=len(history)-1 if history is not None else 0, line_dash="dash", line_color=SEPARATOR_COLOR, line_width=1, annotation_text="today")`.
      - Y-axis percent format `tickformat=".0%"`; x-axis tick labels historical `"M{n}"` / forecast `"+{n}"`.
      - `showlegend=True`, legend bottom-right horizontal.
    - `monte_carlo_fan(...)` public fn: render ChartContainer HTML (title `"Forecast fan chart"`, subtitle `f"{n_simulations:,} simulated paths · P10 / P50 / P90 bands · seed {seed}"`), then `st.plotly_chart(fig, use_container_width=True)`.

    Run the tests — they must now pass.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_components.py::test_transition_heatmap_sparsity tests/unit/test_components.py::test_monte_carlo_fan_traces -x</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/transition_heatmap.py` contains `def transition_heatmap(` and `zmin=0` and `zmax=1`
    - `app/components/transition_heatmap.py` contains `SPARSE_OBS_THRESHOLD` (no bare `< 20` magic number — grep for `SPARSE_OBS_THRESHOLD` returns ≥ 1)
    - `app/components/transition_heatmap.py` contains the warning color `#D97706` and the marker `⚠`
    - `app/components/monte_carlo_fan.py` contains `def monte_carlo_fan(` and `add_vline` and the legend names `Median (P50)`, `P10`, `P90`
    - `grep -c "import streamlit" app/components/transition_heatmap.py` returns ≥ 1 (components ARE app-layer, streamlit allowed here)
    - `uv run pytest tests/unit/test_components.py::test_transition_heatmap_fixed_scale tests/unit/test_components.py::test_transition_heatmap_sparsity tests/unit/test_components.py::test_monte_carlo_fan_traces -x` exits 0 (GREEN)
  </acceptance_criteria>
  <done>Both chart components build figures matching the UI-SPEC contracts (fixed [0,1] heatmap scale + sparsity markers; fan chart with bands + separator + named legend), and the structural tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extend kpi_card and empty_state to the prototype contracts (UI-02, D-06/D-07)</name>
  <files>app/components/kpi_card.py, app/components/empty_state.py, app/components/__init__.py, tests/unit/test_components.py</files>
  <read_first>
    - docs/design-reference/js/ui.jsx (KPICard line 99-122, EmptyState line 222-233 — visual ground truth)
    - .planning/phases/02-design-system-brand-share/02-UI-SPEC.md (§1 kpi_card props + visual contract; §4 empty_state contract; KPI strip table D-09)
    - app/components/kpi_card.py (current stub — has the "Phase 05" TODO to remove per D-07)
    - app/components/empty_state.py (current stub — action tuple signature changes to action_label/action_callback)
    - app/styles/theme.css (after 02-01 port: `.card`, `.accent-card`, `.t-label`, `.t-h3`, `.mono`, `.badge*` classes to reference)
  </read_first>
  <action>
    REWRITE `app/components/kpi_card.py` to the UI-SPEC §1 contract — replace the `st.metric` fallback with custom HTML via `st.markdown(..., unsafe_allow_html=True)`. New signature (keyword-only after the two required positionals):
    ```python
    def kpi_card(
        label: str,
        value: str,
        *,
        unit: str | None = None,
        delta: float | None = None,
        delta_suffix: str = "%",
        sparkline: list[float] | None = None,
        accent: str = "var(--color-primary)",
        icon: str | None = None,
        tooltip: str | None = None,
    ) -> None:
    ```
    HTML structure (single `st.markdown` string): outer `<div class="card accent-card card-pad" style="--accent:{accent}">`; label row `<div class="t-label">{label}</div>` with optional tooltip; value row `<div class="mono" style="font-size:var(--fs-32);font-weight:600;letter-spacing:-0.02em">{value}<span class="t-xs t-ter mono">{unit}</span></div>` (omit unit span if None); delta row only if `delta is not None` — positive uses `color:var(--color-success)` with up-arrow `▲`, negative uses `color:var(--color-danger)` with down-arrow `▼`, format `{arrow} {abs(delta):.1f}{delta_suffix}` in mono 12px weight 600. Empty value: when `value == "—"`, render the `—` in `.t-ter` and SKIP the delta row. Padding `--space-5`. Sparkline: if provided, render a minimal inline SVG polyline (96×28px, stroke 1.5px, color = accent) normalized over the data points; this can be a simple `<svg>` string (no external lib).
    REMOVE the `# TODO(phase05)` comment entirely (D-07). Update the module docstring to drop any Phase 05 reference.

    REWRITE `app/components/empty_state.py` to the UI-SPEC §4 contract. New signature:
    ```python
    def empty_state(
        icon: str,
        title: str,
        description: str,
        *,
        action_label: str | None = None,
        action_callback: Callable | None = None,
    ) -> None:
    ```
    Render custom HTML: centered column `max-width:480px; margin:0 auto; padding:var(--space-7) var(--space-5)`; icon well `56×56px, border-radius:var(--radius-md), background:var(--color-surface-sunken)`, icon centered at 26px in `--color-text-tertiary`; title `.t-h3`; description `.t-sm .t-sec` with `text-wrap:pretty`. If `action_label` is given, render `st.button(action_label, type="primary", on_click=action_callback)` BELOW the HTML block (Streamlit buttons cannot live inside injected HTML).

    UPDATE `app/components/__init__.py`: add `transition_heatmap` and `monte_carlo_fan` imports/exports alongside the existing three. Final `__all__` = `["empty_state", "kpi_card", "monte_carlo_fan", "section_header", "transition_heatmap"]`.

    Add a smoke test to `tests/unit/test_components.py`: `test_kpi_card_no_st_metric` — read `app/components/kpi_card.py` source and assert it does NOT contain `st.metric` and does NOT contain `TODO(phase05)`; assert it contains `accent-card`. `test_empty_state_signature` — `import inspect; from app.components import empty_state; params = inspect.signature(empty_state).parameters`; assert `"action_label" in params` and `"action_callback" in params`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_components.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/kpi_card.py` does NOT contain `st.metric`
    - `app/components/kpi_card.py` does NOT contain `TODO(phase05)` or `phase05`
    - `app/components/kpi_card.py` contains `accent-card` and the keyword-only params `delta_suffix` and `accent`
    - `app/components/empty_state.py` contains `action_label` and `action_callback` in its signature; does NOT contain the old `action: tuple` parameter
    - `app/components/__init__.py` exports `transition_heatmap` and `monte_carlo_fan` (grep both names present)
    - `uv run pytest tests/unit/test_components.py -x -q` exits 0 (all component tests green)
    - `uv run ruff check app/components/` exits 0
  </acceptance_criteria>
  <done>kpi_card and empty_state match the prototype contracts with custom HTML, no st.metric fallback, no Phase 05 TODO; all four components are exported and the component test file is fully green.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_components.py -x -q` exits 0
- `uv run pytest -x -q` full suite green (no regressions)
- `uv run ruff check app/components/` and `uv run mypy` (where configured) clean
- `app/components/__init__.py` re-exports all four components
</verification>

<success_criteria>
- UI-02: four components exist and are exported; chart components testable in isolation via `_build_*_figure` helpers
- BS-02: heatmap has fixed [0,1] scale, per-cell percent annotations, and ⚠ markers on `< 20` obs cells
- BS-03: fan chart has P10/P50/P90 bands, a "today" separator, and a named legend
- D-06/D-07: kpi_card + empty_state use custom HTML to the prototype contract; no st.metric fallback; no stale "Phase 05" TODO
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-brand-share/02-02-SUMMARY.md`
</output>
