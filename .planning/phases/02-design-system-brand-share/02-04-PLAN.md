---
phase: 02-design-system-brand-share
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - app/pages/1_Brand_Share.py
  - tests/unit/test_page_import.py
autonomous: true
requirements: [BS-06]
must_haves:
  truths:
    - "1_Brand_Share.py imports without raising and has no top-level side effects beyond set_page_config / theme injection"
    - "The page has a control strip (dataset select + model picker + horizon slider + Run Forecast button), a KPI strip, and four tabs (Overview / Transition Matrix / Monte Carlo / Model Comparison)"
    - "The heatmap renders on dataset-select; forecast-dependent tabs show empty_state until Run Forecast, then populate; the expensive forecast is gated behind @st.cache_data"
    - "Designed loading, empty, and error states are present (spinner, empty_state, DatasetTooSparseError -> st.error)"
    - "The Overview tab renders the stacked-area market-share forecast (historical + forecast traces with a 'today' separator) built by a private _build_overview_figure helper"
    - "The Overview tab renders the stationary-distribution panel (consuming result.stationary_distribution inside the Overview block, not the fallback warning) on a normally-seeded dataset (ROADMAP SC-5)"
  artifacts:
    - path: "app/pages/1_Brand_Share.py"
      provides: "Fully-wired Brand Share page (BS-06)"
      contains: "st.tabs"
    - path: "tests/unit/test_page_import.py"
      provides: "BS-06 import smoke test + Overview stacked-area structural assertion"
  key_links:
    - from: "app/pages/1_Brand_Share.py"
      to: "domains.brand_share.service.run_forecast (via @st.cache_data wrapper)"
      via: "gated forecast call keyed on (dataset_id, model, horizon, n_simulations, seed)"
      pattern: "cache_data"
    - from: "app/pages/1_Brand_Share.py"
      to: "app.components transition_heatmap / monte_carlo_fan / kpi_card / empty_state"
      via: "tab rendering"
      pattern: "transition_heatmap"
    - from: "app/pages/1_Brand_Share.py"
      to: "app.styles register_theme / inject_theme"
      via: "called at top of page before any chart"
      pattern: "register_theme"
    - from: "app/pages/1_Brand_Share.py (Overview tab)"
      to: "_build_overview_figure stacked-area + add_vline 'today' separator"
      via: "historical + forecast traces with a vertical separator at the last historical period"
      pattern: "add_vline"
    - from: "app/pages/1_Brand_Share.py (Overview tab)"
      to: "result.stationary_distribution"
      via: "stationary panel render block consumes the array (D-13/D-14)"
      pattern: "stationary_distribution"
---

<objective>
Build `app/pages/1_Brand_Share.py` (BS-06): the fully-wired Brand Share page that assembles the design system (02-01), the components (02-02), and the service (02-03) into the 4-tab IA from the prototype. This is pure assembly + state management — all charts come from components, all computation from the service. The page IS the recruiter-facing demo for Phase 02.

Purpose: Deliver the phase goal — a recruiter can open the page, select a dataset, run m1/m2/m3 forecasts, inspect the heatmap, view the Monte Carlo fan chart, and compare model accuracy.
Output: the page file + an import smoke test (BS-06 Nyquist check) that also structurally asserts the Overview stacked-area figure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-brand-share/02-CONTEXT.md
@.planning/phases/02-design-system-brand-share/02-RESEARCH.md
@.planning/phases/02-design-system-brand-share/02-UI-SPEC.md
@.planning/phases/02-design-system-brand-share/02-VALIDATION.md

<interfaces>
<!-- Everything the page consumes. Use these directly — no codebase exploration needed. -->

From app.styles (02-01):
```python
def register_theme() -> None     # registers markovlens Plotly template — call FIRST, after import streamlit
def inject_theme() -> None       # injects theme.css
```

From app.components (02-02):
```python
def transition_heatmap(matrix, obs_counts, state_labels, *, title=..., subtitle=..., period_label=None, height=400) -> None
def monte_carlo_fan(p10, p50, p90, *, history=None, brand_label="Brand", n_simulations=10_000, seed=42, height=400) -> None
def kpi_card(label, value, *, unit=None, delta=None, delta_suffix="%", sparkline=None, accent="var(--color-primary)", icon=None, tooltip=None) -> None
def empty_state(icon, title, description, *, action_label=None, action_callback=None) -> None
```

From domains.brand_share.service (02-03):
```python
def list_datasets(conn) -> list[Dataset]                      # Dataset: id, name, row_count, n_states (NO n_periods on Dataset)
def run_forecast(conn, dataset_id, model_type, horizon, *, n_simulations=10_000, seed=42) -> BrandShareForecastResult
# BrandShareForecastResult fields: forecasts(dict m1/m2/m3 -> ndarray), historical_shares, transition_matrix,
#   recent_transition_matrix, observation_counts, confidence_bands(dict 0.1/0.5/0.9 -> ndarray),
#   stationary_distribution(ndarray|None), accuracy_metrics(dict model -> {mape,brier,log_loss}),
#   backtest_results(list[dict]), state_labels(list[str]), dataset_name, n_transitions, n_periods, best_model
```

From core.db.connection: `get_connection()` returns a DuckDB connection (wrap with @st.cache_resource in the page).
From core.db.queries:
```python
# Dataset is frozen dataclass: id, domain, name, source_path, row_count, n_states — NO n_periods field.
def build_transition_matrix(conn, dataset_id) -> tuple[np.ndarray, np.ndarray]  # cheap heatmap-on-dataset-select (matrix, obs_counts)
def load_transitions(conn, dataset_id, period_range=None) -> pd.DataFrame        # columns: entity_id, period, from_state, to_state, weight
```
NOTE (sub-caption period count): `Dataset` exposes `row_count` (transitions) and `n_states` but NOT a period count. To honor the full UI-SPEC Copywriting Contract sub-caption `"{n:,} transitions · {n} periods · {n} states"` PRE-forecast (on dataset-select, before run_forecast exists), derive the period count cheaply from `load_transitions(conn, ds.id)["period"].nunique()` and cache it in a tiny `@st.cache_data` helper keyed on `dataset_id`. Do NOT pull `n_periods` from the forecast result for this sub-caption — the sub-caption renders before any forecast runs.
From core.exceptions: `DatasetTooSparseError`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Brand Share page shell — config, theme, control strip, KPI strip, tabs, state machine (BS-06)</name>
  <files>app/pages/1_Brand_Share.py</files>
  <read_first>
    - .planning/phases/02-design-system-brand-share/02-UI-SPEC.md (Brand Share Page IA: page config, control strip table, interaction gate D-11/D-15, KPI strip D-09, all 4 tab contents, Page State Machine, Copywriting Contract — use the EXACT copy strings)
    - .planning/phases/02-design-system-brand-share/02-CONTEXT.md (D-08..D-21 — control strip, KPI strip, tabs, gating, caching)
    - .claude/skills/streamlit-page-scaffolder/SKILL.md (page template: set_page_config first, session-state namespacing, error handling) — NOTE: the skill template OMITS `register_theme()`; this task MANDATES calling `register_theme()` right after `set_page_config()` (D-05). Do not rely on the skill template for theme registration.
    - .claude/rules/streamlit-conventions.md (page structure, @st.cache_resource / @st.cache_data, layout patterns, error handling, DO-NOT list)
    - app/Home.py (existing set_page_config pattern to mirror)
    - app/styles/__init__.py (inject_theme + register_theme re-export from 02-01)
  </read_first>
  <action>
    Create `app/pages/1_Brand_Share.py`. Structure (top-level code must be import-safe — no DB calls or chart rendering at module import beyond the standard Streamlit page setup, so the BS-06 import smoke test passes):

    1. Module docstring `"""Brand Share Forecaster page for MarkovLens."""`; `from __future__ import annotations`; `import streamlit as st` FIRST; then numpy, the app.components imports, `from app.styles import inject_theme, register_theme`, `from domains.brand_share import service`, `from core.db.connection import get_connection`, `from core.db.queries import build_transition_matrix, load_transitions`, `from core.exceptions import DatasetTooSparseError`.

    2. `st.set_page_config(page_title="Brand Share — MarkovLens", page_icon="📊", layout="wide", initial_sidebar_state="expanded")` (page_icon emoji is allowed per CLAUDE.md UI rule — emojis only as page icons). Then `register_theme()` (before any chart — D-05) and `inject_theme()`.

    3. Module constants (no magic numbers): `PAGE_NS = "brand_share"`, `DEFAULT_HORIZON = 12`, `HORIZON_MIN, HORIZON_MAX = 1, 24`, `DEFAULT_N_SIMULATIONS = 10_000`, `DEFAULT_SEED = 42`, `SPARSE_OBS_THRESHOLD = 20`, `MODEL_OPTIONS = ["m1", "m2", "m3"]`, and a `MODEL_TOOLTIPS` dict with the exact formula strings from UI-SPEC ("Y₍ₜ₊₁₎ = Yₜ·P — constant transition matrix" etc).

    4. `@st.cache_resource def _get_db(): return get_connection()`.

    5. `@st.cache_data(show_spinner=False) def _cached_forecast(dataset_id: str, model_type: str, horizon: int, n_simulations: int, seed: int)` — calls `service.run_forecast(_get_db(), dataset_id, model_type, horizon, n_simulations=n_simulations, seed=seed)`. NOTE the cache key is exactly `(dataset_id, model_type, horizon, n_simulations, seed)` per D-20; the conn is fetched INSIDE (not a cache arg — it is unhashable). Also add `@st.cache_data(show_spinner=False) def _dataset_period_count(dataset_id: str) -> int: return int(load_transitions(_get_db(), dataset_id)["period"].nunique())` for the control-strip sub-caption (cheap, cached, keyed on dataset_id). Wrap all page-rendering logic in a `def main() -> None:` and call `main()` at the very bottom (Streamlit runs the module top-to-bottom; AppTest in the smoke test runs it the same way). Keep `set_page_config` + `register_theme` + `inject_theme` as the only unconditional top-level calls; all data/render logic lives inside `main()`.

    6. Header: `st.title("Brand Share Forecaster")` + `st.caption("Predict market share evolution from consumer switching data.")`.

    7. Control strip inside `st.container(border=True)`: `col_ds, col_model, col_horizon, col_run = st.columns([3, 2, 2, 1])`.
       - `datasets = service.list_datasets(_get_db())`. If empty: render `empty_state("∅", "No datasets registered", "Run the seed script to populate datasets: uv run python scripts/seed_data.py")` and `st.stop()`.
       - `col_ds.selectbox("Dataset", options=datasets, format_func=lambda d: d.name)`; below it a sub-caption that honors the full UI-SPEC Copywriting Contract: `f"{ds.row_count:,} transitions · {_dataset_period_count(ds.id):,} periods · {ds.n_states} states"` rendered in `.t-xs .t-ter .mono` (via st.caption or st.markdown). The literal word `transitions` MUST appear in this rendered sub-caption string. (Period count is derived from load_transitions per the interfaces note — `Dataset` has no `n_periods`.)
       - `col_model`: `st.segmented_control("Model", options=MODEL_OPTIONS, default="m1", help=...)` (Streamlit 1.57 has it — RESEARCH confirms); fall back to `st.radio("Model", MODEL_OPTIONS, horizontal=True)` only if segmented_control raises AttributeError. Per-option formula tooltip via the `help=` text combining MODEL_TOOLTIPS.
       - `col_horizon.slider("Time horizon", HORIZON_MIN, HORIZON_MAX, DEFAULT_HORIZON, format="%d months")`.
       - `run = col_run.button("Run Forecast", type="primary", use_container_width=True)`.

    8. Session state (namespaced `brand_share.*`): init `st.session_state.setdefault("brand_share.result", None)`. On dataset change, clear the cached result (`brand_share.result = None`) per state machine `dataset_selected`.

    9. On `run`: `with st.spinner("Running forecast across all three models…")`: try `result = _cached_forecast(ds.id, model, horizon, DEFAULT_N_SIMULATIONS, DEFAULT_SEED)`; store in session_state. Except `DatasetTooSparseError as e`: `st.error(str(e))` + `st.info("Try selecting a longer date range or merging states with fewer than 20 observations.")`. Except `Exception as e`: `st.error(f"Unexpected error: {e}")` + `st.info("If this persists, re-run the seed script or check the logs.")`.

    10. KPI strip (D-09): `st.columns(3)` of `kpi_card`. Only populated when `result` exists; otherwise render three cards with value `"—"`. Compute from `result.forecasts[model]`: forecasted leader = `state_labels[argmax(final share)]` (accent = `var(--chart-1)`, no delta); biggest gainer = max positive Δpp vs first-period historical share (accent `var(--chart-2)`, delta_suffix `"pp"`); biggest loser = max negative Δpp (accent `var(--chart-5)`, delta_suffix `"pp"`).

    11. `tab_overview, tab_matrix, tab_mc, tab_compare = st.tabs(["Overview", "Transition Matrix", "Monte Carlo", "Model Comparison"])`. In this task, render the Transition Matrix tab fully (it works pre-forecast) and stub the other three tabs to show `empty_state("📊", "No forecast yet", "Select a dataset and click Run Forecast to generate market share projections.")` when `result is None` (Tasks 2a/2b/2c fill their post-forecast content):
       - **Transition Matrix tab** (renders on dataset-select, cheap — D-11): `matrix, counts = build_transition_matrix(_get_db(), ds.id)`; call `transition_heatmap(matrix, counts, state_labels=...)` where state_labels derived the same way as the service (or pull from result if present). Below: if any `counts < SPARSE_OBS_THRESHOLD`, `st.warning(f"⚠ {n_sparse} cells have fewer than 20 observations — estimates are noisy. Consider merging sparse states or collecting more data.")`. For m2/m3 pass `period_label`.

    12. Bottom of file: `main()` (unconditional call so Streamlit + AppTest run it).
  </action>
  <verify>
    <automated>uv run python -c "import ast; src=open('app/pages/1_Brand_Share.py').read(); ast.parse(src); assert 'register_theme' in src and 'st.tabs' in src and 'transition_heatmap' in src and 'DatasetTooSparseError' in src and 'set_page_config' in src; print('shell-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/pages/1_Brand_Share.py` contains `st.set_page_config(` as the first Streamlit call and `register_theme()` before any `st.plotly_chart`/component call
    - `app/pages/1_Brand_Share.py` contains `st.tabs([` with the four labels `Overview`, `Transition Matrix`, `Monte Carlo`, `Model Comparison`
    - `app/pages/1_Brand_Share.py` contains `@st.cache_data` and `@st.cache_resource`
    - `app/pages/1_Brand_Share.py` contains `transition_heatmap(` and `empty_state(`
    - `app/pages/1_Brand_Share.py` contains the exact copy `Run Forecast`, `No forecast yet`, and `Predict market share evolution from consumer switching data.`
    - `app/pages/1_Brand_Share.py` contains `DatasetTooSparseError` handling
    - `app/pages/1_Brand_Share.py` contains the control-strip sub-caption: grep for a string containing `transitions` AND containing `periods` AND containing `states` (the `"{...} transitions · {...} periods · {...} states"` sub-caption; D-09 / UI-SPEC Copywriting Contract)
    - `uv run ruff check app/pages/1_Brand_Share.py` exits 0
    - The verify command prints `shell-ok` (asserts the shell elements `register_theme`, `st.tabs`, `transition_heatmap`, `DatasetTooSparseError`, `set_page_config` are all present — an empty stub fails this)
  </acceptance_criteria>
  <done>The page shell exists with config, theme registration, control strip with the full transitions/periods/states sub-caption, KPI strip, four tabs, the working Transition Matrix tab, empty/error states, and gated caching. Other three tabs show empty_state pre-forecast.</done>
</task>

<task type="auto">
  <name>Task 2a: Wire the Overview tab — stacked-area forecast (_build_overview_figure) + stationary panel</name>
  <files>app/pages/1_Brand_Share.py</files>
  <read_first>
    - app/pages/1_Brand_Share.py (the shell from Task 1)
    - .planning/phases/02-design-system-brand-share/02-UI-SPEC.md (Tab: Overview two-column stacked-area + stationary panel D-13/D-14 — the stationary panel TITLE is `"Long-run equilibrium"` and the CAVEAT is a SEPARATE subcaption `"Assumes today's transition rates hold indefinitely — a what-if, not a prediction."`, per the UI-SPEC §Tab: Overview component structure. The Copywriting Contract's combined `"Long-run equilibrium (if these rates persist…)"` is the conceptual label; render it SPLIT as the §Tab: Overview structure dictates.)
    - .planning/phases/02-design-system-brand-share/02-RESEARCH.md (Pitfall 7: style native components via config.toml, not CSS — do not fight Streamlit)
    - app/styles/plotly_theme.py (CATEGORICAL_COLORWAY / chart-1..6 colors for brand traces)
  </read_first>
  <action>
    Complete the Overview tab (only rendered when `result is not None`; else keep the empty_state from Task 1). `main_col, side_col = st.columns([0.65, 0.35], gap="large")`:

    **Left — stacked-area market-share forecast.** Factor a private module-level helper `def _build_overview_figure(historical_shares, forecast, state_labels, horizon, model) -> go.Figure:` (page is the app layer — inline Plotly is allowed). It builds a Plotly stacked-area figure from `result.historical_shares` (solid) + `result.forecasts[model]` (dashed forecast), one trace per brand colored chart-1..6 in state order, with a "today" vertical separator at the last historical period via `fig.add_vline(x=<last_hist_index>, line_dash="dash", line_color="rgba(82,82,91,0.5)", annotation_text="today")`. Set the figure title to `"Market share forecast"` and the ChartContainer subtitle to `f"Historical (solid) + {horizon}-month forecast (dashed) · model {model}"`. In the Overview tab body: `fig = _build_overview_figure(result.historical_shares, result.forecasts[model], result.state_labels, horizon, model)` then `st.plotly_chart(fig, use_container_width=True)`.

    **Right — stationary panel (D-13/D-14).** Render a `.card .accent-card` HTML header (`st.markdown`, unsafe_allow_html=True) with:
      - title in `.t-h3`: the exact string `Long-run equilibrium`
      - a SEPARATE caveat subcaption in `.t-xs .t-ter`: the exact string `Assumes today's transition rates hold indefinitely — a what-if, not a prediction.`
    Then consume `result.stationary_distribution` INSIDE this block: if it `is None`, `st.warning("Could not compute stationary distribution for this matrix. The matrix may be absorbing or near-singular.")`; ELSE render a horizontal `go.Bar` (state_labels on y, probability on x, bars colored chart-1..n, value annotations `"{v*100:.1f}%"`, height 280) and `st.plotly_chart(..., use_container_width=True)`. The variable `result.stationary_distribution` MUST be read inside the Overview render block (not merely imported) so SC-5 is satisfied.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_page_import.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/pages/1_Brand_Share.py` contains a private `def _build_overview_figure(` helper
    - `app/pages/1_Brand_Share.py` contains `add_vline` and the chart title string `Market share forecast`
    - `app/pages/1_Brand_Share.py` contains the exact split strings `Long-run equilibrium` AND (separately) `Assumes today` — NOT a single combined `"Long-run equilibrium (if these rates persist…)"` string (matches UI-SPEC §Tab: Overview component structure)
    - `app/pages/1_Brand_Share.py` references `result.stationary_distribution` within the Overview rendering block (grep `stationary_distribution` returns ≥ 1 and it is consumed in an `if ... is None` / `go.Bar` branch, not just imported)
    - `uv run pytest tests/unit/test_page_import.py -x -q` exits 0 (the smoke test from Task 2c asserts the page runs AND `_build_overview_figure` produces a figure with a historical trace + an add_vline/'today' separator shape)
    - `uv run ruff check app/pages/1_Brand_Share.py` exits 0
  </acceptance_criteria>
  <done>The Overview tab renders the stacked-area forecast (via _build_overview_figure with a 'today' separator) and the stationary panel with the split title/caveat, consuming result.stationary_distribution (SC-5).</done>
</task>

<task type="auto">
  <name>Task 2b: Wire the Monte Carlo tab — secondary controls + fan chart + final-state histogram</name>
  <files>app/pages/1_Brand_Share.py</files>
  <read_first>
    - app/pages/1_Brand_Share.py (shell from Task 1 + Overview from Task 2a)
    - .planning/phases/02-design-system-brand-share/02-UI-SPEC.md (Tab: Monte Carlo secondary controls + fan + final-state histogram; Copywriting Contract `"Run Simulation"`, `"Running {n_simulations:,} simulations…"`)
    - app/components/monte_carlo_fan.py (from 02-02 — call, do not reimplement the fan chart)
  </read_first>
  <action>
    Complete the Monte Carlo tab (only rendered when `result is not None`; else keep the empty_state from Task 1). `mc_main, mc_side = st.columns([0.65, 0.35], gap="large")`:
    - Left: a secondary control row (`st.selectbox("Start brand", state_labels)`, `st.slider("Time horizon", HORIZON_MIN, HORIZON_MAX, horizon, format="%d mo")`, `st.slider("Simulations", 1000, 50000, DEFAULT_N_SIMULATIONS, step=1000)`, a seed row with `st.number_input("Seed", value=DEFAULT_SEED)` + `st.button("Randomize")`, and `st.button("Run Simulation", type="primary")`). When the sim button is clicked, recompute via `_cached_forecast` keyed on the local controls inside `st.spinner(f"Running {n_sims:,} simulations…")`, then call `monte_carlo_fan(p10, p50, p90, history=..., brand_label=start_brand, n_simulations=n_sims, seed=seed)` from `result.confidence_bands[0.10/0.50/0.90]`.
    - Right: final-state distribution `go.Bar` (vertical) from the final-step Monte Carlo distribution, ChartContainer title `"Final state distribution"`, subtitle `f"Where {start_brand} lands at +{horizon}mo"`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_page_import.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/pages/1_Brand_Share.py` contains `monte_carlo_fan(` (Monte Carlo tab wired)
    - `app/pages/1_Brand_Share.py` contains the exact strings `Run Simulation` and `Final state distribution`
    - `app/pages/1_Brand_Share.py` references `result.confidence_bands` (fan chart fed from service bands, not recomputed inline)
    - `uv run pytest tests/unit/test_page_import.py -x -q` exits 0
    - `uv run ruff check app/pages/1_Brand_Share.py` exits 0
  </acceptance_criteria>
  <done>The Monte Carlo tab is wired: secondary controls, the fan chart via the monte_carlo_fan component fed by result.confidence_bands, and the final-state histogram.</done>
</task>

<task type="auto">
  <name>Task 2c: Wire the Model Comparison tab + the BS-06 import smoke test (incl. Overview structural check)</name>
  <files>app/pages/1_Brand_Share.py, tests/unit/test_page_import.py</files>
  <read_first>
    - app/pages/1_Brand_Share.py (shell + Overview + Monte Carlo from Tasks 1/2a/2b)
    - .planning/phases/02-design-system-brand-share/02-UI-SPEC.md (Tab: Model Comparison cards + metrics table bold-best-per-column + backtest table + verdict paragraph D-12 + "How to read these metrics" expander)
    - .planning/phases/02-design-system-brand-share/02-VALIDATION.md (BS-06 row: `uv run pytest tests/unit/test_page_import.py -x -q`)
    - .planning/phases/02-design-system-brand-share/02-RESEARCH.md (Pitfall 7: style native components via config.toml, not CSS)
  </read_first>
  <action>
    Complete the Model Comparison tab (only rendered when `result is not None`; else keep the empty_state from Task 1):
    - Three model cards via `st.columns(3)` — each shows model id (mono), formula (from MODEL_TOOLTIPS), and its MAPE/Brier. The card for `result.best_model` gets a `.badge .badge-success` "Best fit" badge (custom HTML) and a `box-shadow: 0 0 0 2px var(--color-primary)` ring. Winner is `result.best_model` (computed by the service — never hardcode).
    - Metrics table: columns `Model | MAPE ↓ | Brier ↓ | Log-loss ↓`. BOLD the best value PER COLUMN (lowest in each column), not per row (BS-04). Render as custom HTML `.tbl` table (mono `.num` right-aligned) OR `st.dataframe` with styling; custom HTML preferred for bold-best control.
    - Walk-forward backtest table from `result.backtest_results` (produced by `walk_forward_backtest`): keep it honest to the actual dict shape (`period, forecast, actual, mape, brier`). Columns `Period | Actual | Forecast | MAPE` (or `Period | Actual | m1 | m2 | m3` only if the result carries all-three forecasts per period).
    - Verdict paragraph (D-12) via `st.markdown`: `f"**{result.best_model}** gives the best overall fit (MAPE {best_mape:.2f}%). {reason}"` where `reason` is selected by best_model: m1 → "A constant-matrix model works well here — brand-switching rates appear stable over the observed periods."; m2 → "A time-varying model captures shifting brand-switching rates that a constant-matrix m1 cannot."; m3 → "The extended model with growth multiplier better captures changes in total market size alongside shifting switching rates." Then a static `st.expander("How to read these metrics", expanded=False)` with the three metric definitions from UI-SPEC (MAPE / Brier / Log-loss).

    **BS-06 import smoke test + Overview structural check** — create `tests/unit/test_page_import.py`:
    - Prefer `from streamlit.testing.v1 import AppTest`: `at = AppTest.from_file("app/pages/1_Brand_Share.py"); at.run()`; assert `not at.exception` (the page runs without raising). If the seeded DB is empty in the test env, the page should hit the "No datasets registered" empty_state and `st.stop()` cleanly — assert that either the page rendered title `"Brand Share Forecaster"` OR stopped on the empty-state without an exception. If `AppTest` proves flaky in this environment, FALL BACK to: `import importlib.util` load the module spec and assert the module object is created without ImportError (a pure import-time smoke check) — document which path was used in the SUMMARY.
    - ADD a structural test for the Overview stacked-area figure (Task 2a's helper) that needs NO Streamlit runtime: `test_overview_figure_has_separator` — import `_build_overview_figure` from the page module (use `importlib` to load `app/pages/1_Brand_Share.py` as a module since the filename starts with a digit), call it with a small synthetic `historical_shares`/`forecast`/`state_labels`, and assert (a) `len(fig.data) >= 1` (at least one historical/brand trace) and (b) a vertical "today" separator exists — either `len(fig.layout.shapes) >= 1` OR an `add_vline`-produced annotation/shape with text/name containing `today`. Loading the page module triggers `register_theme()` + `set_page_config()` at import; guard the import the same way AppTest does (call `at.run()` first, or set `STREAMLIT` test context) — if importing the module standalone raises due to `set_page_config`, instead extract `_build_overview_figure` via `AppTest`'s module namespace or via `importlib` with the Streamlit testing runtime active. Document the chosen mechanism in the test docstring.
    - The command MUST be `uv run pytest tests/unit/test_page_import.py -x -q`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_page_import.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/pages/1_Brand_Share.py` contains `result.best_model` (winner used, not a hardcoded model literal in the verdict)
    - `app/pages/1_Brand_Share.py` contains the exact strings `How to read these metrics` and `Best fit`
    - `app/pages/1_Brand_Share.py` references `result.backtest_results` and contains `MAPE` (metrics + backtest tables wired)
    - `tests/unit/test_page_import.py` exists, uses `AppTest.from_file` OR an importlib import smoke check, and contains a `test_overview_figure_has_separator` (or equivalently named) structural test that asserts the Overview figure has ≥1 trace and a `today` separator
    - `uv run pytest tests/unit/test_page_import.py -x -q` exits 0
    - `uv run ruff check app/pages/1_Brand_Share.py` exits 0
  </acceptance_criteria>
  <done>The Model Comparison tab is wired (cards + bold-best-per-column metrics table + backtest table + computed-best_model verdict + metrics expander), and the BS-06 import smoke test passes with an Overview stacked-area structural assertion.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_page_import.py -x -q` exits 0
- `uv run pytest -x -q` full suite green
- `uv run ruff check app/` clean
- Manual (per 02-VALIDATION Manual-Only): `uv run streamlit run app/Home.py`, open `/Brand_Share`, confirm heatmap colors + sparsity badge, run forecast → fan chart bands + separator + legend, Overview stationary bar chart label (`Long-run equilibrium`) + caveat (`Assumes today's transition rates hold indefinitely…`), and the stacked-area "today" separator
</verification>

<success_criteria>
- BS-06: `1_Brand_Share.py` runs without raising; control strip (with transitions/periods/states sub-caption) + KPI strip + four tabs present; heatmap renders on dataset-select; forecast-dependent tabs gated behind Run Forecast with empty/loading/error states; expensive forecast cached via @st.cache_data keyed on (dataset_id, model, horizon, n_simulations, seed)
- SC-5: the Overview tab renders the stationary-distribution panel (label + caveat) from result.stationary_distribution on a normally-seeded dataset
- Phase goal met end-to-end: select dataset → run m1/m2/m3 → inspect heatmap → view fan chart → compare model accuracy
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-brand-share/02-04-SUMMARY.md`
</output>
