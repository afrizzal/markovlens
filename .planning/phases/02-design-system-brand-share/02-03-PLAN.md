---
phase: 02-design-system-brand-share
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - domains/brand_share/service.py
  - tests/integration/test_brand_share_service.py
autonomous: true
requirements: [BS-01, BS-04]
must_haves:
  truths:
    - "run_forecast(dataset_id, model_type, horizon) returns a BrandShareForecastResult containing only structured NumPy arrays / scalars — no Plotly objects"
    - "list_datasets() returns brand_share-domain datasets from the DB"
    - "accuracy_metrics holds mape/brier/log_loss for each of m1/m2/m3, and the best model is computed from those metrics (never hardcoded)"
    - "state_labels length equals the transition matrix dimension and is index-aligned with build_transition_matrix's internal sort"
  artifacts:
    - path: "domains/brand_share/service.py"
      provides: "BrandShareForecastResult (NumPy-only) + list_datasets + run_forecast orchestration"
      contains: "class BrandShareForecastResult"
    - path: "tests/integration/test_brand_share_service.py"
      provides: "BS-01/BS-04 integration tests against a temp DuckDB"
  key_links:
    - from: "domains/brand_share/service.py run_forecast"
      to: "core.db.queries.build_transition_matrix + core.models M1/M2/M3 + core.simulation.monte_carlo_simulate + core.metrics + compute_stationary"
      via: "orchestration pipeline returning structured arrays"
      pattern: "build_transition_matrix"
    - from: "domains/brand_share/service.py"
      to: "state_labels = sorted(set(from_state)|set(to_state))"
      via: "label derivation matching queries.py internal sort"
      pattern: "sorted\\(set"
---

<objective>
Rewrite `domains/brand_share/service.py` (BS-01) to orchestrate the Phase 01 engine into a single `run_forecast()` returning a NumPy-only `BrandShareForecastResult` — dropping the Plotly-coupled `forecast_chart_json` field that violates D-18. Implement `list_datasets()` (brand_share-filtered) and the per-model accuracy comparison (BS-04). The domain layer stays pure (NO `import streamlit`).

Purpose: A clean contract between the engine and the page. The page receives structured arrays and hands them to the components from 02-02; all Plotly construction happens in app/, never here.
Output: rewritten `service.py` + an integration test against a real temp DuckDB seeded with a small known dataset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-brand-share/02-CONTEXT.md
@.planning/phases/02-design-system-brand-share/02-RESEARCH.md
@.planning/phases/02-design-system-brand-share/02-VALIDATION.md
@core/db/queries.py
@core/models.py
@core/simulation.py
@core/metrics.py

<interfaces>
<!-- Phase 01 core API — call these, do NOT modify them. Signatures verified from source. -->

From core/db/queries.py:
```python
@dataclass(frozen=True)
class Dataset:
    id: str; domain: str; name: str; source_path: str; row_count: int; n_states: int

def list_datasets(conn, domain: str | None = None) -> list[Dataset]: ...
def get_dataset(conn, dataset_id: str) -> Dataset: ...
def load_transitions(conn, dataset_id, period_range=None) -> pd.DataFrame:
    # columns: entity_id, period, from_state, to_state, weight
def build_transition_matrix(conn, dataset_id, *, period=None) -> tuple[np.ndarray, np.ndarray]:
    # returns (matrix shape (n,n) float64 row-stochastic, counts shape (n,n) int64)
    # states discovered via: sorted(set(df["from_state"]) | set(df["to_state"]))   <-- MATCH THIS
```

From core/models.py:
```python
class M1Homogeneous:                # __init__(P); .forecast(Y_1, horizon) -> ForecastResult
class M2TimeVarying:                # __init__(P_t_sequence: ndarray (n_periods,n,n)); .forecast(Y_1, horizon)
class M3Extended:                   # __init__(P_t_sequence, G: ndarray (n,) or (n_periods,n)); .forecast(Q_1, horizon)
def compute_stationary(P) -> np.ndarray | None    # from 02-01
@dataclass(frozen=True)
class ForecastResult: forecast_array: np.ndarray; confidence_bands; model_type: str; horizon: int; accuracy_metrics
```

From core/simulation.py:
```python
def monte_carlo_simulate(matrix, start_state: int | np.ndarray, n_steps=12, n_simulations=10_000, seed=42) -> np.ndarray  # (n_sims, n_steps+1) int64
def calibrate_probability(raw_prob: float) -> float
def compute_quantile_bands(paths, target_extractor, quantiles=(0.10,0.50,0.90)) -> dict[float, np.ndarray]
    # target_extractor MUST return a 2-D (n_sims, n_steps+1) array — e.g. (paths==b).astype(float)
def walk_forward_backtest(df, window=3) -> list[dict]   # keys: period, forecast, actual, mape, brier
```

From core/metrics.py:
```python
def mape(actual, forecast) -> float          # 0-100+ scale, lower better
def brier_score(forecast_prob, actual) -> float
def log_loss(forecast_prob, actual, eps=1e-15) -> float
```

From core/exceptions.py: `DatasetTooSparseError`, `DatasetNotFoundError`.

<!-- The DuckDB connection is owned by the app layer (@st.cache_resource). The service
     functions ACCEPT a `conn` parameter so they stay pure and testable with a temp DB. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redefine BrandShareForecastResult + implement list_datasets and the run_forecast skeleton (BS-01)</name>
  <files>domains/brand_share/service.py, tests/integration/test_brand_share_service.py</files>
  <read_first>
    - domains/brand_share/service.py (current stub — has Plotly `forecast_chart_json` field + wrong "Phase 03" TODOs to remove per D-07/D-19)
    - .planning/phases/02-design-system-brand-share/02-RESEARCH.md (Pattern 4 result shape; Pattern 5 state-labels gap; Pattern 6 per-period matrices; Open Questions 1-3 for Q_1, historical_shares, union states)
    - .planning/phases/02-design-system-brand-share/02-CONTEXT.md (D-18 NumPy-only contract; D-19 orchestration order)
    - core/db/queries.py (build_transition_matrix internal sort at line 294 — labels must match)
    - .claude/rules/python-conventions.md + coding-style.md (frozen dataclass, type hints, no magic numbers, no silent except)
    - tests/conftest.py (temp_duckdb_path fixture)
  </read_first>
  <behavior>
    - run_forecast returns a BrandShareForecastResult; every field is a NumPy array, dict of arrays, list, scalar, or None — no plotly.graph_objects / no dict serialized from a figure
    - state_labels has length == transition_matrix.shape[0] and is sorted(set(from)|set(to))
    - list_datasets(conn) returns only domain == "brand_share" datasets
  </behavior>
  <action>
    First write the FAILING integration test (Task 2 fleshes it out, but stub the core assertions now in `tests/integration/test_brand_share_service.py` marked `@pytest.mark.integration`):
    - Fixture `seeded_conn(temp_duckdb_path)`: open a DuckDB connection, run `core/db/schema.sql`, register a `brand_share` dataset, and `bulk_insert_transitions` a SMALL deterministic long-format frame spanning ≥ 4 periods and 3 states (e.g. states `["A","B","C"]`) with ≥ 20 observations per active cell so no sparsity error fires. Yield the conn.
    - `test_run_forecast_returns_numpy_only`: `result = run_forecast(conn, dataset_id, "m1", horizon=6)`; assert isinstance BrandShareForecastResult; assert `result.transition_matrix.shape == (3,3)`; assert `len(result.state_labels) == 3`; assert `result.state_labels == ["A","B","C"]`; assert all forecast arrays are `np.ndarray`; assert NO field is a `plotly` object (iterate `dataclasses.fields`, assert type name does not start with `plotly`).
    Run it — it FAILS (BrandShareForecastResult still has forecast_chart_json; run_forecast raises NotImplementedError).

    Then REWRITE `domains/brand_share/service.py`:
    - Module docstring (no "Phase 03" reference). `from __future__ import annotations`; imports: dataclasses, numpy as np, duckdb (type only), the core symbols listed in <interfaces>, exceptions.
    - Module constants (NO magic numbers): `BRAND_SHARE_DOMAIN: str = "brand_share"`, `DEFAULT_N_SIMULATIONS: int = 10_000`, `DEFAULT_SEED: int = 42`, `BACKTEST_WINDOW: int = 3`, `MODEL_KEYS: tuple[str, ...] = ("m1", "m2", "m3")`.
    - REPLACE `BrandShareForecastResult` with the NumPy-only frozen dataclass per RESEARCH Pattern 4 (fields: `forecasts: dict[str, np.ndarray]`, `historical_shares: np.ndarray`, `transition_matrix: np.ndarray`, `recent_transition_matrix: np.ndarray`, `observation_counts: np.ndarray`, `confidence_bands: dict[float, np.ndarray]`, `stationary_distribution: np.ndarray | None`, `accuracy_metrics: dict[str, dict[str, float]]`, `backtest_results: list[dict]`, `state_labels: list[str]`, `dataset_name: str`, `n_transitions: int`, `n_periods: int`, `best_model: str`). DELETE `forecast_chart_json`, `kpis`, the old `transition_matrix: list[list[float]]`.
    - `list_datasets(conn: duckdb.DuckDBPyConnection) -> list[Dataset]`: `return queries.list_datasets(conn, domain=BRAND_SHARE_DOMAIN)` (D-19).
    - `run_forecast(conn, dataset_id: str, model_type: str, horizon: int, *, n_simulations: int = DEFAULT_N_SIMULATIONS, seed: int = DEFAULT_SEED) -> BrandShareForecastResult`: implement the data-loading + label derivation portion now (the full pipeline lands in Task 2, but make the test's structural assertions pass):
      - `ds = get_dataset(conn, dataset_id)`.
      - `df = load_transitions(conn, dataset_id)`; `state_labels = sorted(set(df["from_state"]) | set(df["to_state"]))` (MUST match queries.py — RESEARCH Pitfall 2).
      - `matrix, counts = build_transition_matrix(conn, dataset_id)` (m1 constant); `recent` = per-most-recent-period matrix (Task 2 wires m2/m3 fully).
      - Build at least the m1 forecast via `M1Homogeneous`, populate every dataclass field with real arrays (use placeholders only where Task 2 completes them, e.g. set `confidence_bands={}`, `backtest_results=[]`, full `accuracy_metrics` in Task 2). Compute `historical_shares` per RESEARCH Open Question 3 (`df.groupby("period")["from_state"].value_counts(normalize=True)` reshaped to `(n_hist_periods, n_states)` index-aligned to state_labels). Compute `stationary_distribution = compute_stationary(matrix)`. `best_model` placeholder = `"m1"` (Task 2 computes the real winner).
      - `n_transitions = len(df)`, `n_periods = df["period"].nunique()`, `dataset_name = ds.name`.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_brand_share_service.py::test_run_forecast_returns_numpy_only -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `domains/brand_share/service.py` does NOT contain `forecast_chart_json`
    - `grep -c "import streamlit" domains/brand_share/service.py` returns 0 (domain purity — MUST NOT import streamlit)
    - `domains/brand_share/service.py` does NOT contain `Phase 03` or `phase03`
    - `domains/brand_share/service.py` contains `sorted(set(` (state-label derivation matching queries.py)
    - `domains/brand_share/service.py` contains module constant `DEFAULT_N_SIMULATIONS` (no bare `10000`/`10_000` literal in function bodies)
    - `uv run pytest tests/integration/test_brand_share_service.py::test_run_forecast_returns_numpy_only -x -q` exits 0
  </acceptance_criteria>
  <done>BrandShareForecastResult is NumPy-only (Plotly field removed), list_datasets is brand_share-filtered, run_forecast loads data + derives index-aligned state_labels, and the structural integration test passes. Domain stays streamlit-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the run_forecast pipeline + per-model accuracy comparison (BS-04)</name>
  <files>domains/brand_share/service.py, tests/integration/test_brand_share_service.py</files>
  <read_first>
    - domains/brand_share/service.py (the skeleton from Task 1)
    - .planning/phases/02-design-system-brand-share/02-RESEARCH.md (Pattern 6 per-period P_t stacking + Open Question 1 union-states guard; Pattern 9/Pitfall 4 quantile target_extractor; Pitfall 5 M3 absolute counts / Q_1)
    - .planning/phases/02-design-system-brand-share/02-UI-SPEC.md (Model Comparison section: metrics columns + reason-sentence logic D-12)
    - core/simulation.py (monte_carlo_simulate start_state vector form; compute_quantile_bands 2-D extractor requirement; walk_forward_backtest)
    - core/metrics.py (mape/brier_score/log_loss signatures + shapes)
    - .claude/rules/markov-patterns.md (M3 absolute counts vs shares — forbidden practice #5)
  </read_first>
  <behavior>
    - accuracy_metrics["m1"|"m2"|"m3"] each has keys "mape", "brier", "log_loss" (all float)
    - best_model is the model with the lowest MAPE among m1/m2/m3 (computed, not hardcoded)
    - confidence_bands has keys 0.10, 0.50, 0.90, each a 1-D array of length horizon+1, with p10 ≤ p50 ≤ p90 elementwise
    - per-period matrices are unioned to the full state set before stacking (no shape mismatch)
  </behavior>
  <action>
    Complete `run_forecast`:
    - Build the per-period P_t sequence (RESEARCH Pattern 6): `periods = sorted(df["period"].unique())`; for each period call `build_transition_matrix(conn, dataset_id, period=p)`. GUARD per Open Question 1: if a period's matrix has fewer states than the full `n = len(state_labels)`, re-embed it into an `(n, n)` matrix aligned to `state_labels` (zero rows become absorbing self-loops to keep row-stochastic). Stack to `P_t_sequence` shape `(n_periods, n, n)`. `recent_transition_matrix = P_t_sequence[-1]`.
    - Initial share vector `Y_1`: last observed period's `to_state` distribution, normalized, aligned to state_labels. Compute via groupby (RESEARCH Open Question 2/3). For M3, build `Q_1` as ABSOLUTE counts (NOT normalized) per markov-patterns forbidden #5 + Pitfall 5: `Q_1 = df[df.period==max_period].groupby("to_state")["weight"].sum()` aligned to state index; growth multiplier `G = np.ones(n)` (neutral) unless a documented growth is derivable — use `G = np.ones(n)` and note it in a comment.
    - Forecast all three: `M1Homogeneous(matrix).forecast(Y_1, horizon)`, `M2TimeVarying(P_t_sequence).forecast(Y_1, horizon)`, `M3Extended(P_t_sequence, G).forecast(Q_1, horizon)`. Store `forecasts = {"m1": fc1.forecast_array, "m2": fc2.forecast_array, "m3": fc3.forecast_array}`.
    - Monte Carlo confidence bands for the SELECTED `model_type`'s matrix (use `matrix` for m1, `recent_transition_matrix` for m2/m3): `paths = monte_carlo_simulate(P, start_state=Y_1, n_steps=horizon, n_simulations=n_simulations, seed=seed)`. Pick the leading brand index `b = int(np.argmax(Y_1))`; `target_extractor = lambda p, b=b: (p == b).astype(float)` (2-D, per Pitfall 4); `confidence_bands = compute_quantile_bands(paths, target_extractor)`.
    - `backtest_results = walk_forward_backtest(df, window=BACKTEST_WINDOW)`.
    - Per-model accuracy (BS-04): for each model build a one-step-ahead in-sample comparison — using the backtest's last forecast vs actual, or compute against the most-recent-period actual share. For each model produce `{"mape": mape(actual, fc_vec), "brier": brier_score(fc_prob_2d, actual_2d), "log_loss": log_loss(fc_prob_2d, actual_2d)}`. Reshape vectors to 2-D `(1, n)` one-hot/probability rows as `brier_score`/`log_loss` expect. Store `accuracy_metrics = {"m1": {...}, "m2": {...}, "m3": {...}}`.
    - `best_model = min(MODEL_KEYS, key=lambda m: accuracy_metrics[m]["mape"])` (computed winner — D-12).
    - Sparsity: if any cell `< 20` obs the matrix still builds (build_transition_matrix only warns), so do NOT raise here; only raise `DatasetTooSparseError` if `build_transition_matrix` itself raised a ValueError for empty data — wrap the initial build in try/except ValueError and re-raise as `DatasetTooSparseError` with the UI-SPEC copy: `"Dataset is too sparse to build a reliable transition matrix. Try selecting a longer date range or merging states with fewer than 20 observations."`

    Flesh out `tests/integration/test_brand_share_service.py`:
    - `test_model_comparison` (`@pytest.mark.integration`): run forecast on the seeded conn; assert `set(result.accuracy_metrics.keys()) == {"m1","m2","m3"}`; for each model assert keys `{"mape","brier","log_loss"}` present and values are floats; assert `result.best_model in {"m1","m2","m3"}`; assert `result.best_model == min(("m1","m2","m3"), key=lambda m: result.accuracy_metrics[m]["mape"])` (winner is computed, not hardcoded).
    - `test_confidence_bands_monotonic`: assert `set(result.confidence_bands.keys()) == {0.10, 0.50, 0.90}`; assert `(result.confidence_bands[0.10] <= result.confidence_bands[0.50] + 1e-9).all()` and `(result.confidence_bands[0.50] <= result.confidence_bands[0.90] + 1e-9).all()`; assert each band length == horizon + 1.
    - `test_stationary_present`: assert `result.stationary_distribution is None or abs(result.stationary_distribution.sum() - 1.0) < 1e-6`.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_brand_share_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `domains/brand_share/service.py` contains `walk_forward_backtest` call and `compute_quantile_bands` call
    - `domains/brand_share/service.py` contains `best_model = min(` (or equivalent computed-min over MODEL_KEYS — winner not hardcoded)
    - `domains/brand_share/service.py` contains `(p == b).astype(float)` style 2-D extractor (grep `astype(float)` present)
    - `uv run pytest tests/integration/test_brand_share_service.py -x -q` exits 0 (all integration tests green, including `::test_model_comparison`)
    - `grep -c "import streamlit" domains/brand_share/service.py` returns 0
    - `uv run ruff check domains/brand_share/service.py` exits 0
  </acceptance_criteria>
  <done>run_forecast runs the full m1/m2/m3 pipeline (per-period matrices unioned, M3 on absolute counts), produces monotonic Monte Carlo bands, per-model mape/brier/log_loss, a computed best_model, and a backtest — all as NumPy arrays. Integration tests pass.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/integration/test_brand_share_service.py -x -q` exits 0
- `uv run pytest -x -q` full suite green
- `grep -rL "import streamlit" domains/brand_share/service.py` confirms domain purity
- `uv run ruff check domains/` and `uv run mypy domains/` clean
</verification>

<success_criteria>
- BS-01: `BrandShareForecastResult` is NumPy-only (no Plotly); `run_forecast` orchestrates the full pipeline; `list_datasets` is brand_share-filtered; state_labels index-aligned with the matrix
- BS-04: per-model mape/brier/log_loss computed; best_model derived from metrics (never hardcoded)
- Domain layer remains free of `import streamlit`
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-brand-share/02-03-SUMMARY.md`
</output>
